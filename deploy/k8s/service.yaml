apiVersion: v1
kind: Service
metadata:
  name: longcache
  labels:
    app: longcache
spec:
  type: ClusterIP
  selector:
    app: longcache
  ports:
    - name: http
      port: 8000
      targetPort: 8000
