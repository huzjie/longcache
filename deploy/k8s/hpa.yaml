apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: longcache
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: longcache
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
